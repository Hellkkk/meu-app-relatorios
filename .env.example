# Environment Variables

# MongoDB Connection
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/myDatabase?retryWrites=true&w=majority

# JWT Secret Key
JWT_SECRET=your_jwt_secret_key_here

# Backend Configuration
# Use BACKEND_PORT for backend server (recommended)
BACKEND_PORT=5001
BACKEND_HOST=127.0.0.1

# Frontend Configuration  
# Use FRONTEND_PORT for frontend server
FRONTEND_PORT=3001

# Git Commit Hash (auto-generated, do not set manually)
# APP_COMMIT=abc1234

# CORS Configuration
# Add your frontend URLs here
CLIENT_URL=http://localhost:3001
CORS_ORIGIN=http://localhost:3001,http://3.14.182.194:3001,http://3.14.182.194

# Excel Source Path (opcional - se não definido, usa fallbacks)
# EXCEL_SOURCE_PATH=./Compras_AVM.xlsx

# Habilitar auto-importação do Excel no startup do servidor (default: true)
# ENABLE_REPO_EXCEL_BOOTSTRAP=true

# Frontend - Habilitar painel de upload manual (default: false)
# VITE_ENABLE_UPLOAD=false

# AI Configuration (opcional - funcionalidades de IA ficam desabilitadas se não configuradas)
# GOOGLE_API_KEY=your_google_api_key_here
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_EMBEDDING_MODEL=text-embedding-004
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=your_qdrant_api_key_if_needed
# QDRANT_COLLECTION=reports_ai
# AI_MAX_RECORDS_TO_SAMPLE=200

# IMPORTANT: Do NOT set NODE_ENV in .env files that are consumed by Vite/frontend
# NODE_ENV breaks Vite builds. Set NODE_ENV only at runtime via PM2 or command line.
# Example: NODE_ENV=production npm start
