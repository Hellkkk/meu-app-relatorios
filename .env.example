# Environment Variables
# MongoDB Connection
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/myDatabase?retryWrites=true&w=majority

# JWT Secret Key
JWT_SECRET=your_jwt_secret_key_here

# Backend API Server Port
BACKEND_PORT=5001
BACKEND_HOST=127.0.0.1

# Frontend Proxy Server Port
FRONTEND_PORT=3001

# Client URL for CORS (primary frontend URL)
CLIENT_URL=http://localhost:3001

# Additional CORS Origins (comma-separated, optional)
# For production with public IP on port 80 and 3001:
# CORS_ORIGIN=http://3.14.182.194,http://3.14.182.194:3001

# Excel Source Path (opcional - se não definido, usa fallbacks)
# EXCEL_SOURCE_PATH=./Compras_AVM.xlsx

# Habilitar auto-importação do Excel no startup do servidor (default: true)
# ENABLE_REPO_EXCEL_BOOTSTRAP=true

# Frontend - Habilitar painel de upload manual (default: false)
# VITE_ENABLE_UPLOAD=false
