# Configurações de Produção para EC2
NODE_ENV=production

# Backend API Server Port
BACKEND_PORT=5001
BACKEND_HOST=127.0.0.1

# Frontend Proxy Server Port
FRONTEND_PORT=3001

# MongoDB - Use sua string de conexão do Atlas ou local
MONGODB_URI=mongodb://localhost:27017/relatorios
# OU para Atlas:
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/relatorios

# JWT Secret - Gere uma chave segura
JWT_SECRET=sua_chave_jwt_super_secreta_aqui_pelo_menos_32_caracteres

# URLs do Frontend (substitua 3.14.182.194 pelo IP público da sua instância se diferente)
CLIENT_URL=http://3.14.182.194:3001

# CORS Origins - Permite acesso tanto na porta 80 quanto na 3001
# Inclui port 80 (http://3.14.182.194) para acesso via Nginx
CORS_ORIGIN=http://3.14.182.194,http://3.14.182.194:3001

# Configurações de Log
LOG_LEVEL=info